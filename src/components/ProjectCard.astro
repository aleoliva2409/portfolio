---
interface Props {
  title: string;
  description: string;
  technologies: string;
  image?: string;
  codeUrl?: string;
  demoUrl?: string;
}

const { title, description, technologies, image, codeUrl, demoUrl } =
  Astro.props;
---

<article
  class='overflow-hidden rounded-2xl border border-line bg-surface/80 dark:border-line-dark dark:bg-surface-dark/80'
>
  {
    image && (
      <img
        src={image}
        alt={`Captura de ${title}`}
        class='h-48 w-full object-cover'
      />
    )
  }

  <div class='p-5'>
    <h3 class='text-xl font-semibold'>{title}</h3>

    <p class='mt-3 leading-7'>
      {description}
    </p>

    <p class='mt-4 text-sm text-brand dark:text-brand-dark'>
      {technologies}
    </p>

    {
      (codeUrl || demoUrl) && (
        <div class='mt-5 flex gap-3'>
          {codeUrl && (
            <a
              href={codeUrl}
              target='_blank'
              rel='noopener noreferrer'
              class='text-sm font-medium text-accent underline hover:text-brand dark:text-accent-dark dark:hover:text-brand-dark'
            >
              Código
            </a>
          )}

          {demoUrl && (
            <a
              href={demoUrl}
              target='_blank'
              rel='noopener noreferrer'
              class='text-sm font-medium text-accent underline hover:text-brand dark:text-accent-dark dark:hover:text-brand-dark'
            >
              Demo
            </a>
          )}
        </div>
      )
    }
  </div>
</article>
